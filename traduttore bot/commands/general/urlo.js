const { Message } = require("discord.js");

module.exports = {
    name:'urlo',
    data:{
        name:'urlo',
        description: 'Urliamo',
    },
    execute(interaction){
        
        interaction.reply('ululululul!');
    }
}
